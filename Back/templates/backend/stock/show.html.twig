{% extends 'backend/backend_layout.html.twig' %}

{% block title %}Stock{% endblock %}

{% block body %}
    <h1>Stock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock.id }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ stock.stock }}</td>
            </tr>
            <tr>
                <th>Stock_alert</th>
                <td>{{ stock.stockAlert }}</td>
            </tr>
            <tr>
                <th>Product</th>
                <td>{{ stock.product }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stock_index') }}">back to list</a>

    <a href="{{ path('stock_edit', {'id': stock.id}) }}">edit</a>

    {{ include('backend/stock/_delete_form.html.twig') }}
{% endblock %}
