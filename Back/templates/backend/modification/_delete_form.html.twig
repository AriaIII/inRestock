<form method="post" action="{{ path('modification_delete', {'id': modification.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ modification.id) }}">
    <button class="button is-danger">Supprimer</button>
</form>
