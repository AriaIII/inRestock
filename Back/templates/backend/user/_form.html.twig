{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    
    {{ form_widget(form, {'attr': {'class': 'error transparent'}}) }}
    
    <button class="button is-warning">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
