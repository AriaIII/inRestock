{% extends 'backend/backend_layout.html.twig' %}

{% block title %} {{user.lastname}} : modification {% endblock %}

{% block body %}
<nav class="breadcrumb has-succeeds-separator transparent" aria-label="breadcrumbs">
    <ul>
        <li><a href="{{ path('backend_index')}}">Accueil</a></li>
        <li><a href="{{ path('backend_user_index') }}">Utilisateurs</a></li>        
        
        {% if referer == url('backend_user_index') %}
            <li class="is-active"><a href="{{ path('backend_user_edit', {'id' : user.id}) }}" aria-current="page">{{user.lastname}} : modification</a></li>
        {% elseif referer == url('backend_user_show', {'id' : user.id}) %}
            <li><a href="{{ path('backend_user_show', {'id' : user.id}) }}">{{user.lastname}}</a></li>        
            <li class="is-active"><a href="{{ path('backend_user_edit', {'id' : user.id}) }}" aria-current="page">{{user.lastname}} : modification</a></li>
        {% endif %}
    </ul>
</nav>

<div class="new-title">
    <h2 class="title is-3">Modification de la fiche :</h2>
</div>

    {{ include('backend/user/_form.html.twig', {'button_label': 'Valider'}) }}

    <p class="control">
        <a class="button is-primary" href="{{ path('backend_user_index') }}">Retour à la liste</a>
    </p>

    {{ include('backend/user/_delete_form.html.twig') }}

</div>
{% endblock %}