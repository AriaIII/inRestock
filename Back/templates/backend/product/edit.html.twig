{% extends 'backend/backend_layout.html.twig' %}

{% block title %} {{product.name}} : modification {% endblock %}

{% block body %}

<nav class="breadcrumb has-succeeds-separator transparent" aria-label="breadcrumbs">
    <ul>
      <li><a href="{{ path('backend_index')}}">Accueil</a></li>
      <li><a href="{{ path('backend_product_show', {"id" : product.id})}}">{{product.name}}</a></li>
      <li class="is-active"><a href="#" aria-current="page">Modification : {{product.name}}</a></li>
    </ul>
  </nav>

    <div class="new-title">
        <h2 class="title is-3">Modification du produit :</h2>
    </div>

    {{ include('backend/product/_form.html.twig', {'button_label': 'Valider'}) }}
    
    <p class="control">
        <a class="button is-primary" href="{{ path('backend_product_index') }}">Retour à la liste</a>
    </p>
    {{ include('backend/product/_delete_form.html.twig') }}
</div>
{% endblock %}
