{% extends 'backend/backend_layout.html.twig' %}

{% block title %} rôle {{role.name}} : modification{% endblock %}

{% block body %}

<nav class="breadcrumb has-succeeds-separator transparent" aria-label="breadcrumbs">
    <ul>
        <li><a href="{{ path('backend_index')}}">Accueil</a></li>
        <li><a href="{{ path('backend_role_index')}}">Liste des rôles</a></li>
        <li><a href="{{ path('backend_role_show', {"id" : role.id})}}">{{role.name}}</a></li>
        <li class="is-active"><a href="#" aria-current="page">Modification : {{role.name}}</a></li>
    </ul>
</nav>

    <div class="new-title">
        <h2 class="title is-3">Modification du rôle :</h2>
    </div>

    {{ include('backend/role/_form.html.twig', {'button_label': 'Valider'}) }}
    <p class="control">
        <a class="button is-primary" href="{{ path('backend_role_index') }}">Retour à la liste</a>
    </p>
    {{ include('backend/role/_delete_form.html.twig') }}
</div> 
{% endblock %}
