{% extends 'backend/backend_layout.html.twig' %}

{% block title %} rôle {{role.name}} : modification{% endblock %}

{% block body %}
    <div class="new-title">
        <h2 class="title is-3">Modification du rôle :</h2>
    </div>

    {{ include('backend/role/_form.html.twig', {'button_label': 'Valider'}) }}
    <p class="control">
        <a class="button is-primary" href="{{ path('role_index') }}">Retour à la liste</a>
    </p>
    {{ include('backend/role/_delete_form.html.twig') }}
</div> 
{% endblock %}
