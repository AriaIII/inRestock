{% extends 'backend/backend_layout.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h2 class="title is-2">{{role.name}}</h2>

    <table class="table">
    <h3 class="subtitle">Liste des employé </h3>


    <a href="{{ path('role_index') }}">back to list</a>

    <a href="{{ path('role_edit', {'id': role.id}) }}">edit</a>

    {{ include('backend/role/_delete_form.html.twig') }}
{% endblock %}
